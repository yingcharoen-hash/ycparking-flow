
:root {
  --bg-color: #f5f5f7;
  --card-bg: #ffffff;
  --text-main: #1d1d1f;
  --text-muted: #86868b;
  --accent-blue: #0071e3;
  --gold: #d8a032; 
  --gold-lt: #fdf6e3;
  --red: #ff3b30;
  --red-lt: #ffeceb;
  --green: #34c759;
  --green-lt: #ebf9ee;
  --teal: #5ac8fa;
  --teal-lt: #eaf8ff;
  --purple: #af52de;
  --purple-lt: #f4eafe;
  --orange: #ff9500;
  --orange-lt: #fff4e5;
  --gray50: #f5f5f7;
  --gray100: #e5e5ea;
  --gray200: #d1d1d6;
  --gray400: #8e8e93;
  --gray600: #48484a;
  --gray700: #3a3a3c;
  --gray800: #1c1c1e;
  
  --finance-bg: #e8dff5;
  --hr-bg: #ffe4c4;
  --night-bg: #e5e5ea;
}
*{box-sizing:border-box;margin:0;padding:0;}
body{
  font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "Sarabun", "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  background: var(--bg-color);
  color: var(--text-main);
  font-size: 13px;
  -webkit-font-smoothing: antialiased;
  -webkit-print-color-adjust: exact;
  print-color-adjust: exact;
}

/* ===== HEADER ===== */
.header {
  background: #000000;
  color: #f5f5f7;
  padding: 48px 48px 40px;
  position: relative;
  overflow: hidden;
}
.header::after {
  content: '';
  position: absolute;
  top: -50%; left: -50%; width: 200%; height: 200%;
  background: radial-gradient(circle at 50% 0%, rgba(0,113,227,0.15) 0%, transparent 50%);
  pointer-events: none;
}
.header-inner { position: relative; z-index: 1; }
.header-badge {
  display: inline-block;
  background: rgba(255,255,255,0.1);
  border-radius: 20px;
  padding: 6px 16px;
  font-size: 11px;
  font-weight: 600;
  color: #a1a1a6;
  letter-spacing: 0.5px;
  margin-bottom: 12px;
}
.header-title { font-size: 32px; font-weight: 700; color: #ffffff; line-height: 1.25; letter-spacing: -0.5px; }
.header-title em { color: var(--accent-blue); font-style: normal; }
.header-meta { display: flex; gap: 32px; margin-top: 24px; flex-wrap: wrap; }
.meta-item { display: flex; flex-direction: column; gap: 4px; }
.meta-label { font-size: 10px; text-transform: uppercase; letter-spacing: 1px; color: #86868b; font-weight: 600; }
.meta-value { font-size: 13px; color: #f5f5f7; font-weight: 500; }

/* ===== LAYOUT ===== */
.page { display: flex; gap: 0; min-height: calc(100vh - 120px); }

/* ===== SWIMLANE (LEFT) ===== */
.swimlane-col { flex: 1; min-width: 0; overflow-x: auto; padding: 32px 24px 48px 24px; transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1); }
.swimlane-col.has-panel { flex: 0 0 58%; }

.sl-wrap { background: var(--card-bg); border-radius: 24px; box-shadow: 0 12px 32px rgba(0,0,0,0.08); overflow: hidden; border: 1px solid rgba(0,0,0,0.04); min-width: 600px; }
.sl-table { width: 100%; border-collapse: collapse; }

/* Lane label */
.lane-lbl { width: 140px; vertical-align: middle; text-align: center; padding: 20px 16px; font-size: 12px; font-weight: 600; color: var(--text-main); line-height: 1.5; border-right: 1px solid var(--gray200); border-bottom: 1px solid var(--gray200); }
.lane-lbl.counter { background: var(--teal-lt); }
.lane-lbl.ops { background: #fafafc; }
.lane-lbl.hr { background: var(--hr-bg); }
.lane-lbl.fin-field { background: var(--finance-bg); }
.lane-lbl.fin-recv { background: var(--purple-lt); }
.lane-lbl.night { background: var(--night-bg); }
.lane-lbl.audit { background: var(--purple-lt); }
.lane-lbl.hdr { background: var(--gray100); color: var(--text-muted); font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; border-top-left-radius: 24px; }

/* Steps cell */
.sl-steps { padding: 24px; vertical-align: middle; border-bottom: 1px solid var(--gray200); }
.sl-steps.audit-bg { background: rgba(175, 82, 222, 0.05); }
.sl-steps.counter-bg { background: rgba(90, 200, 250, 0.05); }
.sl-steps.hr-bg { background: rgba(255, 149, 0, 0.05); }
.sl-steps.fin-bg { background: rgba(175, 82, 222, 0.05); }

/* Flow row */
.flow { display: flex; align-items: center; flex-wrap: wrap; gap: 0; }
.flow-item { display: flex; align-items: center; }

/* Step card */
.step { position: relative; border-radius: 16px; padding: 16px; border: none; cursor: pointer; transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1); min-width: 140px; max-width: 175px; user-select: none; background: #f5f5f7; box-shadow: inset 0 0 0 1px rgba(0,0,0,0.05); }
.step:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.06), inset 0 0 0 1px rgba(0,0,0,0.02); }
.step.active { outline: none; transform: translateY(-4px) scale(1.02); box-shadow: 0 0 0 2px var(--accent-blue), 0 12px 32px rgba(0,113,227,0.15); }

.step.ops { background: #f0f4f8; }
.step.risk { background: var(--red-lt); box-shadow: inset 0 0 0 1px rgba(255,59,48,0.2); }
.step.ctrl { background: var(--green-lt); }
.step.fin { background: var(--purple-lt); }
.step.teal { background: var(--teal-lt); }
.step.hr-card { background: var(--orange-lt); }
.step.night-card { background: var(--gray100); }

.step-num { font-size: 10px; font-weight: 600; color: var(--text-muted); margin-bottom: 6px; letter-spacing: 0.5px; text-transform: uppercase; }
.step-text { font-size: 13px; font-weight: 600; color: var(--text-main); line-height: 1.5; }
.step.risk .step-text { color: var(--red); }

/* Badge */
.badge { position: absolute; top: -8px; right: -8px; font-size: 10px; font-weight: 600; padding: 4px 8px; border-radius: 12px; white-space: nowrap; pointer-events: none; }
.badge.r { background: var(--red); color: #fff; box-shadow: 0 4px 12px rgba(255,59,48,0.3); }
.badge.c { background: var(--green); color: #fff; box-shadow: 0 4px 12px rgba(52,199,89,0.3); }
.badge.w { background: var(--orange); color: #fff; box-shadow: 0 4px 12px rgba(255,149,0,0.3); }

/* Arrow */
.arr { color: var(--gray400); font-size: 18px; padding: 0 12px; flex-shrink: 0; font-weight: 300; }
.arr-dot { color: var(--gray400); font-size: 14px; padding: 0 12px; flex-shrink: 0; }

/* Case boxes inside ops lane */
.case-box { background: #f5f5f7; border-radius: 16px; padding: 16px; border: 1px solid rgba(0,0,0,0.04); flex: 1; min-width: 150px; }
.case-box.b { background: #f0f4f8; }
.case-box.c { background: var(--green-lt); }
.case-title { font-size: 11px; font-weight: 600; margin-bottom: 12px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.5px; }
.case-box.b .case-title { color: var(--accent-blue); }
.case-box.c .case-title { color: var(--green); }

/* Inline warning strips */
.strip { border-radius: 8px; padding: 10px 14px; font-size: 12px; font-weight: 500; margin-top: 8px; line-height: 1.5; }
.strip.y { background: var(--orange-lt); color: var(--orange); }
.strip.r { background: var(--red-lt); color: var(--red); }

/* ===== DETAIL PANEL (RIGHT) ===== */
.panel-col { width: 0; overflow: hidden; transition: width 0.4s cubic-bezier(0.16, 1, 0.3, 1); flex-shrink: 0; }
.panel-col.open { width: 42%; }

.panel { background: rgba(255, 255, 255, 0.85); backdrop-filter: blur(40px) saturate(200%); -webkit-backdrop-filter: blur(40px) saturate(200%); border-radius: 24px; margin: 32px 24px 48px 0; box-shadow: 0 20px 60px rgba(0,0,0,0.12), 0 0 0 1px rgba(0,0,0,0.05); overflow: hidden; height: calc(100% - 80px); display: flex; flex-direction: column; }
.panel-head { background: transparent; padding: 24px 24px 20px; display: flex; justify-content: space-between; align-items: flex-start; flex-shrink: 0; border-bottom: 1px solid rgba(0,0,0,0.08); }
.panel-head-left { flex: 1; }
.panel-step-id { font-size: 11px; font-weight: 600; color: var(--text-muted); letter-spacing: 0.5px; text-transform: uppercase; margin-bottom: 6px; }
.panel-title { font-size: 18px; font-weight: 600; color: var(--text-main); line-height: 1.3; }
.panel-close { width: 32px; height: 32px; background: rgba(0,0,0,0.05); border: none; border-radius: 50%; color: var(--text-main); font-size: 18px; cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; margin-left: 16px; transition: background 0.2s; }
.panel-close:hover { background: rgba(0,0,0,0.1); }

.panel-body { flex: 1; overflow-y: auto; padding: 24px; }
.panel-body::-webkit-scrollbar { width: 6px; }
.panel-body::-webkit-scrollbar-thumb { background: rgba(0,0,0,0.15); border-radius: 10px; }

.panel-section { margin-bottom: 24px; }
.panel-section-title { font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; color: var(--text-muted); margin-bottom: 12px; padding-bottom: 8px; border-bottom: 1px solid var(--gray200); }

.task-list { display: flex; flex-direction: column; gap: 12px; }
.task-item { display: flex; gap: 12px; align-items: flex-start; }
.task-dot { width: 8px; height: 8px; background: var(--accent-blue); border-radius: 50%; margin-top: 6px; flex-shrink: 0; }
.task-text { font-size: 14px; color: var(--text-main); font-weight: 500; line-height: 1.6; }

.doc-list { display: flex; flex-direction: column; gap: 8px; }
.doc-item { background: rgba(255,255,255,0.6); border: 1px solid rgba(0,0,0,0.04); border-radius: 12px; padding: 12px 16px; font-size: 13px; font-weight: 500; color: var(--text-main); box-shadow: 0 2px 8px rgba(0,0,0,0.02); }

.warning-box { background: var(--red-lt); border-radius: 16px; padding: 16px 20px; }
.warning-title { font-size: 11px; font-weight: 600; color: var(--red); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 8px; }
.warning-text { font-size: 13px; font-weight: 500; color: #a32020; line-height: 1.6; }

.info-box { background: var(--teal-lt); border-radius: 16px; padding: 16px 20px; }
.info-text { font-size: 13px; font-weight: 500; color: #0e4a60; line-height: 1.6; }

.freq-tag { display: inline-flex; align-items: center; gap: 6px; background: var(--text-main); color: #fff; padding: 6px 14px; border-radius: 20px; font-size: 12px; font-weight: 600; }

.resp-list { display: flex; flex-direction: column; gap: 8px; }
.resp-item { display: flex; align-items: center; gap: 12px; font-size: 14px; font-weight: 500; color: var(--text-main); }
.resp-dot { width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; }

/* ===== RISK TABLE (BELOW) ===== */
.risk-section { padding: 0 24px 64px; }
.risk-section-title { font-size: 16px; font-weight: 600; color: var(--text-main); margin-bottom: 24px; display: flex; align-items: center; gap: 12px; }
.risk-badge-hd { background: var(--red-lt); color: var(--red); padding: 4px 12px; border-radius: 12px; font-size: 11px; font-weight: 600; }

.risk-tbl { width: 100%; border-collapse: separate; border-spacing: 0; border-radius: 20px; overflow: hidden; box-shadow: var(--shadow-lg); background: #fff; border: 1px solid rgba(0,0,0,0.04); }
.risk-tbl th { background: #fafafc; color: var(--text-muted); padding: 16px 20px; font-size: 12px; font-weight: 600; text-align: left; letter-spacing: 0.5px; text-transform: uppercase; border-bottom: 1px solid var(--gray200); }
.risk-tbl td { background: #fff; padding: 16px 20px; font-size: 13px; border-bottom: 1px solid var(--gray100); vertical-align: top; line-height: 1.6; color: var(--text-main); }
.risk-tbl tr:last-child td { border-bottom: none; }
.risk-tbl tr:hover td { background: #fcfcfd; }
.rl { display: inline-block; padding: 4px 12px; border-radius: 12px; font-size: 11px; font-weight: 600; }
.rl.h { background: var(--red-lt); color: var(--red); }
.rl.v { background: #ff0000; color: #fff; }
.rl.m { background: var(--orange-lt); color: var(--orange); }

/* formula box */
.formula { background: #f0f4f8; border-radius: 16px; padding: 24px; margin-bottom: 32px; border: 1px solid rgba(0,0,0,0.04); }
.formula-title { font-size: 12px; font-weight: 600; color: var(--text-muted); margin-bottom: 16px; text-transform: uppercase; letter-spacing: 0.5px; }
.formula-body { background: #fff; border-radius: 12px; padding: 16px 20px; font-size: 14px; font-weight: 600; color: var(--text-main); line-height: 2.2; box-shadow: 0 2px 12px rgba(0,0,0,0.02); }
.f-green { color: var(--green); }
.f-teal { background: var(--teal-lt); padding: 2px 8px; border-radius: 8px; color: var(--teal); font-weight: 600; }
.f-blue { background: #e0edff; padding: 2px 8px; border-radius: 8px; color: #1a4a8e; font-weight: 600; }
.f-orange { background: var(--orange-lt); padding: 2px 8px; border-radius: 8px; color: var(--orange); font-weight: 600; }
.f-red { color: var(--red); font-weight: 600; }

/* critical box */
.critical { background: var(--red-lt); border: 1px solid rgba(255,59,48,0.2); border-radius: 16px; padding: 20px 24px; margin-bottom: 24px; }
.critical-title { font-size: 13px; font-weight: 600; color: var(--red); margin-bottom: 8px; display: flex; align-items: center; gap: 8px; text-transform: uppercase; letter-spacing: 0.5px; }
.critical-text { font-size: 14px; font-weight: 500; color: #a32020; line-height: 1.7; }

/* ======= PRINT ======= */
@media print {
  @page { size: A4 landscape; margin: 10mm; }
  
  /* Hide interactive / unnecessary stuff */
  .nav-links, .panel-close, .badge { display: none !important; }
  
  /* Reset header */
  .header { padding: 0 !important; background: none !important; color: #000 !important; border-bottom: 2px solid #e5e5ea; }
  .header-title, .meta-label, .meta-value { color: #000 !important; }
  .header::after { display: none; }
  .header-badge { color: #000; border: 1px solid #000; }
  
  /* Reset layout */
  body { background: #fff !important; }
  .page { display: block !important; height: auto !important; min-height: 0 !important; overflow: visible !important; }
  .swimlane-col { display: block !important; padding: 0 !important; overflow: visible !important; width: 100% !important; flex: none !important; }
  .sl-wrap { border: 1px solid #000; box-shadow: none !important; }
  
  /* Risk Section */
  .risk-section { display: block !important; padding: 24px 0 0 !important; page-break-inside: avoid; }
  .risk-tbl, .formula, .critical { border: 1px solid #000 !important; box-shadow: none !important; }
  
  /* Detail Panel on next page */
  .panel-col { 
    display: block !important; 
    width: 100% !important; 
    overflow: visible !important; 
    page-break-before: always; /* Details start on next page */
    margin-top: 20px;
  }
  .panel {
    display: block !important; 
    margin: 0 !important; 
    box-shadow: none !important; 
    border: 1px solid #000 !important; 
    height: auto !important; 
    background: #fff !important;
    backdrop-filter: none !important;
  }
  .panel-body { overflow: visible !important; padding: 24px !important; }
  .panel-head { background: #f5f5f7 !important; border-bottom: 1px solid #000 !important; }
  .panel-title { color: #000 !important; }
  .panel-step-id { color: #000 !important; }
  
  * { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; }
}
